package duncan.atkinson.inventory;

/**
 * As we have very few products an enum here will suffice but could change if we were to increase
 * the number of products.
 */
public enum ProductId {
    SIM_CARD,
    PHONE_CASE,
    PHONE_INSURANCE,
    WIRED_EARPHONES,
    WIRELESS_EARPHONES,
    ;

}
